module.exports = {
  project: {
    ios: {},
    android: {},
  },
  // Bundled Poppins font family (matches the Figma design). Linked into the
  // Android/iOS build via `npx react-native-asset`.
  assets: ['./assets/fonts'],
};
